<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.john.kimberlyclark">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_launcher_new"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".DetailsProcessActivity"
            android:label="@string/title_activity_details_process"
            android:theme="@style/AppTheme" />
        <activity android:name=".GraphicActivity"></activity>
    </application>

</manifest>